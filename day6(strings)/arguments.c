#include<stdio.h>

int main(int argc,char *argv[]);
{
    char argc=("a.exe","abc","CSE");
    argc=strlen(arg);

    for(int i=0;i<argc;i++)
    {
        printf("%s\n",argv[i]);
    }
    return 0;
}